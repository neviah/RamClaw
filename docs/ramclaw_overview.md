# RamClaw Overview

RamClaw packages a fully sandboxed, local-LLM-only fork of OpenClaw as a Pinokio app. It bundles modified OpenClaw code, forces LM Studio as the sole LLM provider, and keeps all tools while removing cloud providers. The sandbox lives under `/sandbox`, with work products in `/sandbox/workspace`.
